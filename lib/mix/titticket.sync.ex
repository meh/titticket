defmodule Mix.Tasks.Titticket.Sync do
  use Mix.Task

  @shortdoc "Synchronize event and ticket status with the database."

  @doc false
  def run(args) do
    # TODO: Update event and ticket status based on the opens/closes dates.
  end
end
